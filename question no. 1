// Q1: Write a program to input two numbers and display thier sum.
#include <stdio.h>

int main()
{
    int a, b;
    printf("enter the two numbers:");
    scanf("%d %d", &a, &b);
    int c =a + b;
    printf("sum of %d and %d = %d", a, b, c);

    return 0;
}
